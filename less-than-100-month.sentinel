import "tfrun"
import "decimal"

delta_monthly_cost = decimal.new(tfrun.cost_estimate.delta_monthly_cost)

monthly_cost = decimal.new(tfrun.cost_estimate.monthly_cost)

main = rule {
    monthly_cost.less_than(100)
}

